# START HERE

1. Create an empty GitHub repository named `global-economic-complexity-scanner`.
2. Push this project to `main`.
3. Import it into Vercel.
4. No API secrets are required for V1.
5. Verify `/health`, `/snapshot`, and one `/country/{iso3}` route.

The live application must never substitute fabricated ECI/PCI data if the Atlas provider is unavailable.
